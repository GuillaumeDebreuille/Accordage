{% include 'layout/header.html.twig' %}

<h1>Coucou</h1>

{% for product in products %}

id : {{ product.id }} //
name : {{ product.name }} // 
category : {{ product.category }} // 
description : {{ product.description }} // 
prix : {{ product.price }}€ //
stock : {{ product.stock }} //
occasion ? : {{ product.isUsed }} // 
image : <img src="{{ asset('uploads/products/' ~ product.images[0]) }}" style="width:120px; height:120px; object-fit:contain;" >
<br>
{% endfor %}

